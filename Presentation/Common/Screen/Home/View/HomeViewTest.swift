import SwiftUI

struct HomeViewTest: View {
    
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(Color("YellowPrimary"))
            Text("Hello, world!")
        }
        .padding()
    }
}
